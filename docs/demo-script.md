# 示範影片分鏡

<!-- generated by scripts/demo_script.py; edit the table there, not here -->

13 幕、63 句。每一句是一段字幕（中文最多 16 字一行、兩行；英文 42 字元一行），顯示在頁面**下方的黑帶**裡，不蓋畫面；「框」那一欄是字幕上的時候框起來的元素。無聲版每句停留「秒」欄那麼久（中文 4.5 字/秒的配音速度），合計約 5.3 分鐘，還沒算模型回答的等待——等待中先講「（等模型時）」那幾句，剩下的等待在成片裡加速（預設 3 倍，畫面幾乎不動，右上角標 >> 3x），所以成片比錄的短。

錄：`uv run --with playwright python scripts/demo_record.py --lang zh-TW --stem 00041208 --index 15 --silent --base http://127.0.0.1:8111`（先用 `--check` 走一遍每一頁，確認要框的元素都在）。無聲版帶字幕、沒有聲音；旁邊的 `docs/demo/build/<lang>/script.md` 是同一份台詞加上每句的秒數，配音對著它講。確認台詞之後 `--narrate-existing` 把旁白（video_transfer 的 Qwen3-TTS／Kokoro）配上去，每句一段、聽回來檢查。

每次錄影會把示範用的區域按 0 退回；`00041208` 目前待判的是 #15。

| 句 | 秒 | 框 | 中文 | English |
|---|---|---|---|---|
| intro.0 | 6.3 s | `#f-aoi` | AOI：自動光學檢測，Automated Optical Inspection。 | AOI: Automated Optical Inspection. |
| intro.1 | 6.9 s | `#f-aoi` | AOI 是產線上拍照找瑕疵的機器；設定上寧可誤報，也不漏檢。 | It is the camera that checks boards; it over-flags rather than miss a defect. |
| intro.2 | 6.3 s | `#f-lead` | AOI 標出的區域，六成是誤報，卻每一個都要人工複判。 | Six in ten flagged regions are false calls, and every one goes to a person. |
| intro.3 | 3.8 s | `#f-system` | 本系統接在既有的 AOI 之後。 | It sits behind the AOI the line already has. |
| intro.4 | 2.5 s | `#f-model` | 視覺模型先判定。 | A vision model decides first. |
| intro.5 | 3.6 s | `#f-agent` | 沒把握的，由 agent 接手。 | What it is unsure of goes to an agent. |
| intro.6 | 4.6 s | `#f-operator` | 兩者都無法判定的區域，才交給作業員。 | Only what neither can settle reaches an operator. |
| intro.7 | 6.2 s | `#f-label` | 作業員的判定會記錄下來，作為下一輪訓練的標註資料。 | The operator's verdict is recorded, as the label for the next training round. |
| intro.8 | 4.6 s | `#f-ask` | 主管也可直接用中文，詢問產線的問題。 | A supervisor can ask the line a question in plain words. |
| cli.0 | 6.0 s | `#hdr` | 這是工程師看的執行紀錄：一片 PCB，每個區域一行。 | The engineer's log of the run: one PCB, one line per flagged region. |
| cli.1 | 7.3 s | `#queued` | 多數區域模型幾毫秒內就判定完成；信心不足的兩個，交給作業員。 | Most the model settles in milliseconds; the two it is unsure of go to an operator. |
| cli.2 | 4.7 s | `#board` | 整片 PCB 先扣留，待複判完成再處置。 | The whole board is held until they answer. |
| login.0 | 5.0 s | `form.signin-form` | 作業員登入後，每一筆判定都記錄在其名下。 | An operator signs in. Every verdict from here on carries this name. |
| home.0 | 5.0 s | `.home h1` | 登入後先看到主畫面：這條產線目前的狀態。 | The first screen is the line as it stands right now. |
| home.1 | 5.8 s | `.home .stats:first-of-type` | 已檢驗的 PCB：已處置、扣留、放行、待判各幾片。 | The PCBs that ran: dispositioned, held, released, still waiting. |
| home.2 | 5.9 s | `.home .stats:first-of-type` | 放行為 0 並非異常：示範資料每片都含真實瑕疵。 | Released 0 is not a fault: every demo board really carries a defect. |
| home.3 | 5.3 s | `.home .stats:last-of-type` | 以及待人工複判、待資深複判的區域各有幾個。 | How many regions wait on a person, and how many went back to a senior. |
| home.4 | 5.7 s | `.home .doors` | 主畫面先呈現整條線的狀態；待複判清單在下一層。 | The front page shows the line first; the review queue is one level in. |
| queue.0 | 5.5 s | `table.queue` | 無法判定的區域都列在這裡；等候最久的排最前。 | Every region the agent could not settle is here, longest wait on top. |
| queue.1 | 5.5 s | `table.queue tbody tr:first-child` | 每一列一個區域：判定類別、信心值、誤判機率。 | Each row: the model's call, how sure it is, how likely a false call. |
| queue.2 | 5.7 s | `table.queue tbody tr:first-child td.reason` | 右側是 agent 的說明；agent 只解釋，不判定。 | On the right, the agent's rationale. The agent explains; it never decides. |
| queue.3 | 5.6 s | `table.queue tbody tr:first-child td.reason` | 實測過：改由 LLM 決定，準確度比分類模型低。 | Measured: letting the LLM decide was less accurate than the classifier. |
| region.0 | 2.6 s | `.station .head` | 開啟其中一個區域。 | Open one region. |
| region.1 | 4.5 s | `.handover` | 最上方：agent 交付人工複判的理由。 | At the top: why the agent handed it over. |
| region.2 | 4.4 s | `figure.triptych` | golden image、待測 PCB、差異圖。 | Golden image, PCB under test, difference. |
| region.3 | 4.9 s | `figure.triptych` | 左側三張影像，紅框是 AOI 標出的位置。 | Three images on the left; the red box is where the AOI flagged. |
| region.4 | 6.8 s | `.columns .col:nth-child(1)` | 模型的判讀：類別、信心值，及實際分析的 64 px 小圖。 | What the model read: class, confidence, and the 64 px window it actually saw. |
| region.5 | 5.9 s | `.columns .col:nth-child(2)` | 這台機台最近的缺陷率（複判後），與其他機台比較。 | This machine's recent defect rate after re-verification, against the fleet. |
| region.6 | 5.0 s | `.columns .col:nth-child(3)` | 此瑕疵類別的驗收標準，只取該類別的文件。 | The acceptance criteria for this class, and only this class. |
| region.7 | 3.3 s | `form.verdict` | 作業員以數字鍵輸入判定。 | The operator answers with a number key. |
| region.8 | 4.6 s | `form.verdict p.sub.dim` | 資料集附有標準答案，此頁刻意不顯示。 | The dataset comes with an answer key; this page deliberately hides it. |
| region.9 | 4.8 s | `form.verdict p.sub.dim` | 若先看到答案，判定就不再是獨立的判斷。 | Seen first, the answer would replace the operator's own judgement. |
| defer.0 | 2.8 s | `form.defer .defer-button` | 無法判斷時，按 0。 | Can't tell? Press zero. |
| defer.1 | 5.0 s | `table.queue tbody tr@@{region}` | 這不會記錄為判定；區域改列入待資深複判。 | It is not recorded as a verdict. The region moves to the senior list. |
| blocked.0 | 4.6 s | `.station .head` | 改以一般作業員登入，開啟同一個區域。 | Sign in as an ordinary operator and open the same region. |
| blocked.1 | 3.7 s | `section.station > p.sub.warn-line` | 判定按鈕消失，只剩一行說明。 | The buttons are gone; one sentence remains. |
| blocked.2 | 6.8 s | `section.station > p.sub.warn-line` | 退回的區域只有資深人員能判定；這是示範站唯一的權限設定。 | Handed-back regions are for seniors; this is the demo's one permission. |
| boards.0 | 5.2 s | `section.panel > p:nth-of-type(2)` | PCB 處置紀錄：已處置、扣留、放行、待判。 | PCB dispositions: dispositioned, held, released, waiting. |
| boards.1 | 5.7 s | `section.panel > p:nth-of-type(2)` | 上方的統計是整個資料表的合計，不是本頁的列數。 | These counts are totals over the whole table, not the rows on this page. |
| boards.2 | 5.4 s | `table.queue tbody tr:first-child` | 每片 PCB：處置結果、區域數、判定者、時間。 | Each PCB: its disposition, how many regions, who decided, and when. |
| ask.0 | 3.5 s | `form.ask`（邊打字） | 主管的問題，直接用中文問。 | A supervisor's question, typed in plain language. |
| ask.1 | 5.0 s | `#progress`（等模型時） | 系統先將問題轉成查詢計畫，驗證後才執行。 | The question becomes a plan of lookups, validated before anything runs. |
| ask.2 | 5.3 s | `#progress`（等模型時） | 規劃：由模型決定查詢哪些工具、帶哪些參數。 | Planning: the model picks which tools to call, and with what. |
| ask.3 | 5.0 s | `#progress`（等模型時） | 驗證：參數不在資料庫的實際值內，即拒絕。 | Validation: an argument the store does not hold is refused. |
| ask.4 | 5.7 s | `#progress`（等模型時） | 查詢僅需幾毫秒；其餘等待時間是模型在撰寫回答。 | The lookups return in milliseconds; the rest of the wait is the model writing. |
| ask.5 | 7.0 s | `figure.chart` | 前後兩根長條，信賴區間不重疊：參數變更後 open 佔比下降。 | Two bars, before and after, intervals apart: the share of opens fell. |
| ask.6 | 5.3 s | `figure.chart` | 圖表依結果的形狀自動產生，不是由模型挑選。 | The chart is derived from the shape of the results, not chosen by the model. |
| ask.7 | 5.5 s | `figure.chart + .answer-block` | 說明文字緊鄰數據，每個數字都能對回查詢結果。 | The prose sits beside the numbers; every figure in it is checked against them. |
| control.0 | 4.3 s | `form.ask`（邊打字） | 再問一個對照組：M31 換燈前後。 | Now a control: the lamp replacement on M31. |
| control.1 | 4.6 s | `#progress`（等模型時） | 同一套流程，換一台機台、換一個事件。 | The same flow, on another machine and another event. |
| control.2 | 5.3 s | `figure.chart` | 兩個信賴區間重疊，系統即回答沒有顯著差異。 | The intervals overlap, and the system says so. |
| control.3 | 3.0 s | `figure.chart` | 有事件，不代表有影響。 | An event is not an effect. |
| switch.0 | 2.5 s | `nav.locale a`（邊打字） | 切換語言。 | Switch the language. |
| switch.1 | 4.4 s | `first:.answer-block h2 .as-asked` | 問題與規劃段保留原文，並加上標示。 | The question and the plan stay as written, and are labelled. |
| switch.2 | 4.6 s | `form.rewrite` | 回答不會自動改寫，需按下按鈕才重寫。 | The answer does not change by itself; it is written again on request. |
| switch.3 | 6.2 s | `form.rewrite`（等模型時） | 重寫走的是同一條實測過的流程，因此需再等模型一次。 | The rewrite takes the same measured path, so it waits on the model once. |
| switch.4 | 6.2 s | `figure.chart + .answer-block` | 以儲存的同一批結果重新撰寫；不是翻譯，原文仍保留。 | Written again from the stored results, not translated; the original is kept. |
| outro.0 | 4.6 s | `` | 人工複判的工作量，減少 55.6%。 | Manual review work down by 55.6%. |
| outro.1 | 5.5 s | `` | 漏檢率 0.66%，高於 0.5% 的預算。 | Escape 0.66%, above the 0.5% budget. |
| outro.2 | 5.5 s | `` | 漏掉的開路，電性測試可再攔截；其餘在預算內。 | The escaped opens are caught by electrical test; the rest is within budget. |
| outro.3 | 5.7 s | `` | README 如實記載：信賴區間、各類別、五組種子。 | The README records the interval, each class, and five seeds. |
| outro.4 | 4.2 s | `` | 每個門檻都能追溯到產生它的腳本。 | Every threshold traces back to the script that produced it. |
| outro.5 | 4.2 s | `` | 每個數字，都在 benchmarks 裡。 | Every figure is in the benchmarks file. |

## 畫面（錄影器做的事）

| 幕 | 畫面 |
|---|---|
| intro | 片頭卡：標題、一句問題、處置流程圖 |
| cli | 終端機：`board 00041208 --queue` 的輸出，只留開頭三個區域、三個進佇列的、最後的 HELD 一行 |
| login | `/login` 停 1 秒，打字登入 `mike`（資深） |
| home | `/` 主畫面 |
| queue | `/queue`，捲一點 |
| region | `/c/00041208/<index>`：三聯圖 → 捲到三欄證據 → 捲到判定按鈕 |
| defer | 同頁按 `0` → `/deferred` |
| blocked | 登出、`watcher`（一般作業員）登入、同一區域、捲到底 |
| boards | 再以 `mike` 登入、`/boards` |
| ask | `/ask` 打 M32 的問題、等進度面板、等圖 |
| control | 再問 M31 換燈、等圖 |
| switch | 按另一個語言 → 按「重寫」→ 等新答案 |
| outro | 片尾卡：三個數字、repo 網址 |
