# Dataset — clone it, don't vendor it (231 MB)
data/DeepPCB/
# Derived artefacts, rebuilt by scripts/build_patches.py
data/patches/
# Trained weights and predictions
models/
# Evaluation output
eval/results/

__pycache__/
*.py[cod]
.venv/
.DS_Store
.pytest_cache/
# The station's operator file. Passphrases are stored as PBKDF2 records rather
# than in the clear, which is not a reason to publish one.
data/operators
data/aoi_agent.db
data/aoi_agent.db.bak-*
data/checkpoints.db*
data/chroma/
